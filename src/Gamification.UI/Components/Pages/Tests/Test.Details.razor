<!-----------------------------------------------------------------------
// This file is autogenerated by EntityCore
// <auto-generated />
------------------------------------------------------------------------>

@page "/tests/{id:int}"
@rendermode InteractiveServer
@using System
@using Gamification.Domain.Entities
@using DataTransferObjects.Tests
@using Services.Tests
@inject ITestsService TestService
@inject NavigationManager NavigationManager

<h3>Test Details</h3>

@if (test == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <dl class="row">
        <dt class="col-sm-3">Title</dt>
        @if(@test.Title != default)
        {
            <dd class="col-sm-9">@test.Title</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Description</dt>
        @if(@test.Description != default)
        {
            <dd class="col-sm-9">@test.Description</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Duration</dt>
        @if(@test.Duration != default)
        {
            <dd class="col-sm-9">@test.Duration</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Status</dt>
        @if(@test.Status != default)
        {
            <dd class="col-sm-9">@test.Status</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">CreatedBy</dt>
        @if(@test.CreatedBy != default)
        {
            <dd class="col-sm-9">@test.CreatedBy</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Created</dt>
        @if(@test.Created != default)
        {
            <dd class="col-sm-9">@test.Created</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">LastModifiedBy</dt>
        @if(@test.LastModifiedBy != default)
        {
            <dd class="col-sm-9">@test.LastModifiedBy</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">LastModified</dt>
        @if(@test.LastModified != default)
        {
            <dd class="col-sm-9">@test.LastModified</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

    </dl>
    <div>
        <button class="btn btn-primary" @onclick="Save">Save</button>
        <button class="btn btn-secondary" @onclick="BackToList">Back to List</button>
    </div>
}

@code {
    [Parameter]
    public int Id { get; set; }

    private TestViewModel? test;

    protected override async Task OnParametersSetAsync()
    {
        test = await TestService.GetByIdAsync(Id);
    }

    private void Save()
    {
        throw new NotImplementedException("Save functionality is not implemented.");
    }

    private void BackToList()
    {
        NavigationManager.NavigateTo("/tests");
    }
}
