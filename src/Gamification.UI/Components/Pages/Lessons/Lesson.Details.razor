<!-----------------------------------------------------------------------
// This file is autogenerated by EntityCore
// <auto-generated />
------------------------------------------------------------------------>

@page "/lessons/{id:int}"
@rendermode InteractiveServer
@using System
@using DataTransferObjects.Lessons
@using Services.Lessons
@inject ILessonsService LessonService
@inject NavigationManager NavigationManager

<h3>Lesson Details</h3>

@if (lesson == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <dl class="row">
        <dt class="col-sm-3">Title</dt>
        @if(@lesson.Title != default)
        {
            <dd class="col-sm-9">@lesson.Title</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Description</dt>
        @if(@lesson.Description != default)
        {
            <dd class="col-sm-9">@lesson.Description</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">CreatedBy</dt>
        @if(@lesson.CreatedBy != default)
        {
            <dd class="col-sm-9">@lesson.CreatedBy</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Created</dt>
        @if(@lesson.Created != default)
        {
            <dd class="col-sm-9">@lesson.Created</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">LastModifiedBy</dt>
        @if(@lesson.LastModifiedBy != default)
        {
            <dd class="col-sm-9">@lesson.LastModifiedBy</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">LastModified</dt>
        @if(@lesson.LastModified != default)
        {
            <dd class="col-sm-9">@lesson.LastModified</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

    </dl>
    <div>
        <button class="btn btn-primary" @onclick="Save">Save</button>
        <button class="btn btn-secondary" @onclick="BackToList">Back to List</button>
    </div>
}

@code {
    [Parameter]
    public int Id { get; set; }

    private LessonViewModel? lesson;

    protected override async Task OnParametersSetAsync()
    {
        lesson = await LessonService.GetByIdAsync(Id);
    }

    private void Save()
    {
        throw new NotImplementedException("Save functionality is not implemented.");
    }

    private void BackToList()
    {
        NavigationManager.NavigateTo("/lessons");
    }
}
