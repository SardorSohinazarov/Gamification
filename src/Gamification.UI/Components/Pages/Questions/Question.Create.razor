<!-----------------------------------------------------------------------
// This file is autogenerated by EntityCore
// <auto-generated />
------------------------------------------------------------------------>

@page "/questions/create"
@rendermode InteractiveServer
@using System
@using Gamification.Domain.Entities
@using Services.Questions
@inject IQuestionsService QuestionService
@inject NavigationManager NavigationManager

<h3>Create New Question</h3>

<EditForm FormName="Question" Model="@question" OnValidSubmit="() => HandleValidSubmit()">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="form-group mb-3">
        <label for="text">Text:</label>
        <InputText id="text" class="form-control" @bind-Value="question.Text" />
        <ValidationMessage For="@(() => question.Text)" />
    </div>
    <div class="form-group mb-3">
        <label for="testid">TestId:</label>
        <InputNumber id="testid" class="form-control" @bind-Value="question.TestId" />
        <ValidationMessage For="@(() => question.TestId)" />
    </div>
    @* <div class="form-group mb-3"> *@
    @*     <label for="test">Test:</label> *@
    @*     <InputText id="test" class="form-control" @bind-Value="question.Test" /> *@
    @*     <ValidationMessage For="@(() => question.Test)" /> *@
    @* </div> *@
    <div class="form-group mb-3">
        <label for="createdby">CreatedBy:</label>
        <InputText id="createdby" class="form-control" @bind-Value="question.CreatedBy" />
        <ValidationMessage For="@(() => question.CreatedBy)" />
    </div>
    <div class="form-group mb-3">
        <label for="created">Created:</label>
        <InputDate id="created" class="form-control" @bind-Value="question.Created" />
        <ValidationMessage For="@(() => question.Created)" />
    </div>
    <div class="form-group mb-3">
        <label for="lastmodifiedby">LastModifiedBy:</label>
        <InputText id="lastmodifiedby" class="form-control" @bind-Value="question.LastModifiedBy" />
        <ValidationMessage For="@(() => question.LastModifiedBy)" />
    </div>
    <div class="form-group mb-3">
        <label for="lastmodified">LastModified:</label>
        <InputDate id="lastmodified" class="form-control" @bind-Value="question.LastModified" />
        <ValidationMessage For="@(() => question.LastModified)" />
    </div>
    <div class="form-group mb-3">
        <label for="id">Id:</label>
        <InputNumber id="id" class="form-control" @bind-Value="question.Id" />
        <ValidationMessage For="@(() => question.Id)" />
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <button type="button" class="btn btn-secondary" @onclick="Cancel">Cancel</button>
</EditForm>

@code {
    [SupplyParameterFromForm]
    public Question question { get; set; } = new Question();

    private async Task HandleValidSubmit()
    {
        await QuestionService.AddAsync(question);
        NavigationManager.NavigateTo("/questions");
    }

    private void Cancel()
    {
        NavigationManager.NavigateTo("/questions");
    }
}
