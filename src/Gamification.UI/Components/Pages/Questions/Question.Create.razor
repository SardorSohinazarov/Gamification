<!-----------------------------------------------------------------------
// This file is autogenerated by EntityCore
// <auto-generated />
------------------------------------------------------------------------>

@page "/questions/create"
@rendermode InteractiveServer
@using System
@using System.Collections.Generic
@using DataTransferObjects.Questions
@using Services.Questions
@inject IQuestionsService QuestionService
@inject NavigationManager NavigationManager

<h3>Create New Question</h3>

<EditForm FormName="QuestionCreationDto" Model="@question" OnValidSubmit="() => HandleValidSubmit()">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="form-group mb-3">
        <label for="text">Text:</label>
        <InputText id="text" class="form-control" @bind-Value="question.Text" />
        <ValidationMessage For="@(() => question.Text)" />
    </div>
    <div class="form-group mb-3">
        <label for="testid">TestId:</label>
        <InputNumber id="testid" class="form-control" @bind-Value="question.TestId" />
        <ValidationMessage For="@(() => question.TestId)" />
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <button type="button" class="btn btn-secondary" @onclick="Cancel">Cancel</button>
</EditForm>

@code {
    [SupplyParameterFromForm]
    public QuestionCreationDto question { get; set; } = new QuestionCreationDto();

    private async Task HandleValidSubmit()
    {
        await QuestionService.AddAsync(question);
        NavigationManager.NavigateTo("/questions");
    }

    private void Cancel()
    {
        NavigationManager.NavigateTo("/questions");
    }
}
