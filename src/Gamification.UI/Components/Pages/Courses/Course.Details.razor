<!-----------------------------------------------------------------------
// This file is autogenerated by EntityCore
// <auto-generated />
------------------------------------------------------------------------>

@page "/courses/{id:int}"
@rendermode InteractiveServer
@using System
@using DataTransferObjects.Courses
@using Services.Courses
@inject ICoursesService CourseService
@inject NavigationManager NavigationManager

<h3>Course Details</h3>

@if (course == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <dl class="row">
        <dt class="col-sm-3">Title</dt>
        @if(@course.Title != default)
        {
            <dd class="col-sm-9">@course.Title</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Description</dt>
        @if(@course.Description != default)
        {
            <dd class="col-sm-9">@course.Description</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Duration</dt>
        @if(@course.Duration != default)
        {
            <dd class="col-sm-9">@course.Duration</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">StartTime</dt>
        @if(@course.StartTime != default)
        {
            <dd class="col-sm-9">@course.StartTime</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">StartDate</dt>
        @if(@course.StartDate != default)
        {
            <dd class="col-sm-9">@course.StartDate</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">CreatedBy</dt>
        @if(@course.CreatedBy != default)
        {
            <dd class="col-sm-9">@course.CreatedBy</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">Created</dt>
        @if(@course.Created != default)
        {
            <dd class="col-sm-9">@course.Created</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">LastModifiedBy</dt>
        @if(@course.LastModifiedBy != default)
        {
            <dd class="col-sm-9">@course.LastModifiedBy</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

        <dt class="col-sm-3">LastModified</dt>
        @if(@course.LastModified != default)
        {
            <dd class="col-sm-9">@course.LastModified</dd>
        }
        else
        {
            <dd class="col-sm-9">None</dd>
        }

    </dl>
    <div>
        <button class="btn btn-primary" @onclick="Save">Save</button>
        <button class="btn btn-secondary" @onclick="BackToList">Back to List</button>
    </div>
}

@code {
    [Parameter]
    public int Id { get; set; }

    private CourseViewModel? course;

    protected override async Task OnParametersSetAsync()
    {
        course = await CourseService.GetByIdAsync(Id);
    }

    private void Save()
    {
        throw new NotImplementedException("Save functionality is not implemented.");
    }

    private void BackToList()
    {
        NavigationManager.NavigateTo("/courses");
    }
}
